This is a basic python script that copies .raw files from one location to another. It won't transfer files if they keep getting larger. There are options within the GUI to change the time and which files it transfers (regex).


You'll need to install pystray pillow.

> pip install pystray pillow

To initiate run the following command

> python raw_file_monitor.py



